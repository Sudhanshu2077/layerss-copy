{
  "name": "layerss",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "prebuild": "node -e \"require('fs').copyFileSync('server/data/menu.json', 'api/menu-data.json')\"",
    "build": "npm install --prefix client && npm run build --prefix client",
    "start": "node server/index.js"
  }
}

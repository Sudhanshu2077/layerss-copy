# Copy to .env.local for local dev. Never commit real URLs with secrets.
# Backend API base (empty = same-origin, uses Vite proxy in dev).
# Production example: https://layerss-backend.onrender.com
VITE_API_URL=

# Public ordering/contact (safe to expose — already public on the site).
VITE_WA_NUMBER=917378777740
VITE_INSTAGRAM_URL=https://www.instagram.com/layerss_thebakehouse/
VITE_MAP_EMBED_URL=https://maps.google.com/maps?q=Chhatrapati%20Sambhajinagar%2C%20Maharashtra&output=embed
